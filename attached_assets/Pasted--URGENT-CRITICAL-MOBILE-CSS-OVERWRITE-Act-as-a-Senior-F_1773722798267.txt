# [URGENT: CRITICAL MOBILE CSS OVERWRITE]

Act as a Senior Frontend Developer. The mobile UI is completely broken. 
**Instruction:** Find the main CSS file (e.g., `style.css`) and APPEND the following code at the very bottom to override all previous broken styles. 

**DO NOT CHANGE THE LOGIC. ONLY APPLY THESE VISUAL FIXES.**

```css
/* FORCED MOBILE OVERRIDES */
@media (max-width: 768px) {
  /* 1. HOME VIEWPORT FIX */
  .hero-section {
    height: 100vh !important;
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
    padding: 20px !important;
    position: relative !important;
    overflow: hidden !important;
  }
  .hero-title { font-size: 26px !important; margin-bottom: 8px !important; }
  .hero-subtitle { font-size: 13px !important; line-height: 1.4 !important; }
  
  /* 2. BUTTONS & SCROLL INDICATOR SEPARATION */
  .cta-buttons { 
    display: flex !important; 
    flex-direction: column !important; 
    gap: 8px !important; 
    width: 100% !important; 
    margin-top: 15px !important; 
    margin-bottom: 80px !important; /* Space for scroll guide */
  }
  .cta-button { height: 48px !important; width: 100% !important; font-size: 14px !important; }
  
  .scroll-indicator {
    position: absolute !important;
    bottom: 90px !important;
    left: 50% !important;
    transform: translateX(-50%) !important;
    display: flex !important;
    flex-direction: column !important;
    align-items: center !important;
    z-index: 9999 !important;
  }

  /* 3. BATTLE & RADIO COMPACT VIEW */
  .video-container, iframe {
    max-height: 25vh !important;
    width: 100% !important;
    margin: 5px 0 !important;
  }
  .vote-btn, .start-btn {
    height: 45px !important;
    margin: 5px 0 !important;
    font-size: 14px !important;
  }

  /* 4. BOTTOM NAVIGATION CLEANUP */
  .bottom-nav {
    height: 65px !important;
    display: flex !important;
    justify-content: space-around !important;
    align-items: center !important;
    padding-bottom: env(safe-area-inset-bottom) !important;
    background: rgba(0, 0, 0, 0.95) !important;
  }
  .nav-item i { font-size: 18px !important; }
  .nav-item span { font-size: 9px !important; display: block !important; }

  /* 5. CREATORS GRID FIX */
  .creators-grid { display: flex !important; flex-direction: column !important; gap: 10px !important; }
  .creator-card { width: 100% !important; min-height: 160px !important; }
}